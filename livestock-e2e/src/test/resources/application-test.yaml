spring:
  datasource:
    url: jdbc:h2:mem:livestock_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        transaction:
          jta:
            platform: no
  flyway:
    enabled: true
    locations: classpath:db/migration/test
  profiles:
    active: test